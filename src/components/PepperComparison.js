import React, { Fragment } from "react";

export const PepperComparison = props => {
  console.log("the hero we don't deserve", props.selectedPepperRows);
  return (
    <Fragment>
      <h2>Pepper Comparison</h2>
    </Fragment>
  );
};
